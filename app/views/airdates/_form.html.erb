<%= form_for(@airdate) do |f| %>
  <% if @airdate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airdate.errors.count, "error") %> prohibited this airdate from being saved:</h2>

      <ul>
      <% @airdate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :airdate %><br />
    <%= f.datetime_select :airdate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
